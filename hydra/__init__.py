"""Project HYDRA — automated vulnerability discovery & exploitation confirmation framework.

For authorized security testing only. Every outgoing request is validated against the
configured engagement scope before transmission (see hydra.utils.scope).
"""

__version__ = "0.1.0"
